
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@model  List<Project_Entity.Category>
<div class="row">
    <div class="col-lg-12">
        <h5 class="page-header"><a href="/Admin/Category/Add" class="btn btn-success">Yeni Ekle</a></h5>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Kategoriler
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Resim</th>
                                <th>Kategori Adı</th>
                                <th>Oluşturulma Tarihi</th>
                                <th>Durum</th>
                                <th>İşlemler</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr id="sil_@item.ID" class="odd gradeX">
                                    <td><img src="@item.ImagePath" width="50" height="50" /></td>
                                    <td>@item.Title</td>
                                    <td>@item.CreatedDate</td>
                                    @if (item.IsActive)
                                    {
                                        <td id="durum_@item.ID" onclick="Status(@item.ID,'/Admin/Category/Status/')"><span class='btn btn-success'>Aktif</span></td>
                                    }
                                    else
                                    {
                                        <td id="durum_@item.ID" onclick="Status(@item.ID,'/Admin/Category/Status/')"><span class='btn btn-danger'>Pasif</span></td>
                                    }
                                    <td>
                                        <a href="/Admin/Category/Edit/@item.ID" class="glyphicon glyphicon-edit"></a>
                                        <a href="#" onclick="Delete(@item.ID,'/Admin/Category/Remove/')" class="glyphicon glyphicon-remove"></a>
                                    </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
