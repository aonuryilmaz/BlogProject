
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@model  List<Project_Entity.Tag>
<div class="row">
    <div class="col-lg-12">
        <h5 class="page-header"><a href="/Admin/Tag/Add" class="btn btn-success">Yeni Ekle</a></h5>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
             Etiketler
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Etiket Adı</th>
                                <th>Oluşturulma Tarihi</th>
                                <th>Durum</th>
                                <th>İşlemler</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr id="sil_@item.ID" class="odd gradeX">
                                    <td>@item.Title</td>
                                    <td>@item.CreatedDate</td>
                                    @if (item.IsActive)
                                    {
                                        <td id="durum_@item.ID" onclick="Status(@item.ID,'/Admin/Tag/Status/')"><span class='btn btn-success'>Aktif</span></td>
                                    }
                                    else
                                    {
                                        <td id="durum_@item.ID"  onclick="Status(@item.ID,'/Admin/Tag/Status/')"><span class='btn btn-danger'>Pasif</span></td>
                                    }
                     <td>
                         <a href="/Admin/Tag/Edit/@item.ID" class="glyphicon glyphicon-edit"></a>
                         <a href="#" onclick="Delete(@item.ID,'/Admin/Tag/Remove/')" class="glyphicon glyphicon-remove"></a>
                     </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
